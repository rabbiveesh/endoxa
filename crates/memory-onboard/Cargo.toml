[package]
name = "memory-onboard"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Tiered onboarding harness. Tier 0: deterministic git-history lead harvest (no LLM, no writes)."

[lib]
path = "src/lib.rs"

[dependencies]
memory-embed = { path = "../memory-embed" }
serde_json = "1"
