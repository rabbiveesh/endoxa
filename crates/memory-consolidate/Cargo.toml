[package]
name = "memory-consolidate"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Consolidation: linkers (propose edges) + the consolidator (sole writer) + orchestrator."

[lib]
path = "src/lib.rs"

[dependencies]
memory-core = { path = "../memory-core" }
memory-embed = { path = "../memory-embed" }
