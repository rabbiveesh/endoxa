> **Generated artifact** — `corpus-grounding-harvest` workflow (R4 collect, 2026-06).
> 6 corpora harvested (robot-game, perl-openapi-client, tree-sitter-pod, zed-perl,
> composr, tree-sitter-perl); private-crm + sql-abstract + helix + perl-lsp hit a transient
> rate-limit and are resumable. No private the private corpus content here → committable.

# Grounded Question Bank + Round Fetch-Record
## Two flavors: Design-Rationale ("why X over Y") and Kludge / Known-Debt

Scope: 6 corpora (robot-game, perl-openapi-client, tree-sitter-pod, zed-perl, composr, tree-sitter-perl). private-crm did not appear in this round's harvest data; where it would surface it is private and must be marked **(private)**.

---

## 1. Design-Rationale Question Bank

### 1A. Adaptive-learning / domain-modeling rationale (robot-game)

| Question | Chosen | Alternative | Grounded rationale (refs) | Status |
|---|---|---|---|---|
| Why accuracy-window promotion instead of streak-based (N-in-a-row)? | Promote on center acc ≥75% over 4+ AND stretch ≥60% over 2+; streak is display-only, zero mechanical effect | Hard bands + streak-to-promote, or "either streak OR accuracy" hybrid (initial spec) | Streaks caused 3 observed CLI-sim failures: promotion cliffs, oscillation (gifted profile bounced bands 5–6 for 30 Qs), floor traps (0.65³=27% odds). Window measures sustained perf one error can't break. Hybrid rejected: blending already kills the cliff streaks solved → pure complexity. `docs/adr/001-band-blending.md:7-49`; `learner_profile.rs (should_promote/should_demote)` | **GAP** |
| Why model band difficulty as a probability distribution (center + spread_width) vs hard levels / half-bands? | Band = center of bell-curve; spread_width (0.5) tightens on frustration, widens on success; stretch ramps via CRA | Half-bands (5.5/6.5) interpolating ops; or hard bands w/ window widened 20→50 | Half-bands double band count without fixing promotion; bigger window only delays oscillation. Distribution gives free interleaving (spaced-practice). Cost: complex promote/demote logic; `accuracy_at_band(new_center)` reads near-zero post-change (intentional, "looks like a bug without the comment"). `adr/001`; `challenge_generator.rs:9-54`; `rolling_window.rs` | **GAP** |
| Why the cross-domain boundary as a Rust path-dep with zero serialization vs a JSON/WASM bridge? | game depends on domain via path dep, consumes types directly; field add = one compiler-checked refactor | v2: domain→WASM, JS presentation, JSON adapter bridge | JS-adapter abandoned in macroquad migration (PR #15). Hybrid forced camelCase fixes, WASM load-timing hacks, OperationStats drift (flat vs nested) — commits 81daab/788f6a2/b861391. Pure-Rust deletes the whole serialization failure class. Cost: presentation fields (craLevelShown/answerMode/hintUsed) "nulls for now". `architecture-spec.md:26`; `operation-taxonomy-spec.md:373,484`; `git:ac48738` | **GAP** |

### 1B. Test-architecture rationale (robot-game)

| Question | Chosen | Alternative | Grounded rationale (refs) | Status |
|---|---|---|---|---|
| Why pure step()/render() split + in-memory event log vs real macroquad headless? | Pure `Game::step(&FrameInput,dt,screen)` (no macroquad::*), render-only `Game::render`; input via FrameInput, RNG via seeded SmallRng, save behind SaveBackend trait; tests assert on GameEvent log; plain `cargo test`, <0.1s | macroquad headless (harness=false) w/ fake events; domain-only (62 reducer tests); Game generic over render trait | Headless macroquad needs objc shim on macOS, ties speed to event loop, won't run display-less CI. Domain-only misses GLUE bugs (34 scattered is_key_pressed, 7 unseeded macroquad::rand, save link failures). Render trait rejected: tests assert state+events, not draws. Cost: Game grew to ~1,600 lines. `docs/adr/002-headless-test-harness.md`; `game.rs (step/render)`; `input.rs (FrameInput)` | **GAP** |
| Why a single MapDef trait taking `&mut SmallRng` even though every map is static? | `world::MapDef` trait: `build(&mut SmallRng)->Map` + exits/area_name/roster; per-map seed `FNV(world_seed,id)`; new top-level `world` module above tilemap+npc | Keep `tilemap::maps` dispatch, let tilemap reach into npc; add rng later when procedural ships | `&mut rng` is a forward-compat seam: static maps ignore it, procedural carves from it, no call site changes when first generated map lands. Also fixes a layering inversion (MapMeta.roster made engine reach up into npc). Validated additively (`world_matches_legacy`: byte-identical). `git:f036c78`, `git:62e28d7`; `robot-buddy-game/src/world/` | **GAP** |
| Why pin miniquad to a git commit vs a crates.io release? | Cargo.toml pins miniquad git commit 350cb91b6f13 | Latest tagged crates.io release | Pinned commit carries a macOS window-focus bug fix not yet in any release; published crate ships the bug. Constraint-driven, revert-when-released. Corpus has the FACT of the pin; the trade-off framing is the rationale layer. `Cargo.toml:9-18`; belief `miniquad-pinned-commit` | **covered (fact only)** |

### 1C. Class/codegen & API-surface rationale (perl-openapi-client)

| Question | Chosen | Alternative | Grounded rationale (refs) | Status |
|---|---|---|---|---|
| Generate a real package via string-eval vs a Moose/Moo metaclass or AUTOLOAD? | string-eval `package $class; use Mojo::Base '$parent'; 1;` + per-route monkey_patch | metaobject protocol; or dispatch-hash + AUTOLOAD | Zero non-core/Mojo deps; real named subs so `can()` works (call()/call_p() rely on it); plays with `isa()` caching and ordinary subclassing/roles. Cost: eval-string w/ confess; class name is sanitized/md5'd URL; monkey_patch O(routes) once, then cached. `Client.pm:68-74,88-89,27` | **covered** |
| Accept a bare-string `base_url` ctor arg or require a Mojo::URL? | Accept either: coerce non-blessed scalar to Mojo::URL in new() | Require Mojo::URL (original 1.03 POD showed only post-construction mutation) | Contested w/ visible revert chain: PR #33 (0d648f7) rewrote POD to Mojo::URL-only; 200c2d5 reverted AND added the `blessed` coercion so the documented string form truly works. blessed-check matters because base_url can already be a Mojo::URL from the validator. Corpus has the doc-revert beliefs but not the *why*. `Client.pm:45`; `git:200c2d5,0d648f7` | **GAP** (why-layer) |
| Fix generated-subclass parent to OpenAPI::Client (`$BASE=__PACKAGE__`) or derive from invocant? | Derive `$parent` from invocant; `use Mojo::Base '$parent'` | Keep `$BASE` constant, always inherit base | 810f532 (1.04, #35/#37) removed `$BASE`. With it hardwired, subclassing to add roles/methods before new() silently failed. Trade-off: cache key now namespaced `$self::...` (line 239) → same spec under two parents = two cached classes (correct, but caching is per-parent, not per-spec). Corpus has subclass-hardwired/fixed but not the per-parent cache-key consequence. `git:810f532`; `Client.pm:38-42,239` | **GAP** (consequence-layer) |
| Surface client-side validation error as a thrown error/object, or as a Mojo::Transaction? | Synthesize a fake 400 Mojo::Transaction::HTTP (JSON `{errors=>[...]}`, error `{message,code:400}`) so caller's `$tx->error`/`$tx->res` path is identical to a server 400 | croak/die; or return a bare error list callers special-case | One error model for client+server failures across blocking/non-blocking/promise APIs. Cost: fabricate a tx by hand, keep it consistent — f43fe79 was a follow-up bug fix because the fake tx forgot `$tx->req->method`. Corpus has the tx-400 belief but not the f43fe79 "keep forgetting to make the fake tx look real" follow-up. `Client.pm:196-205,101-108`; `git:f43fe79` | **covered** (gap: f43fe79 nuance) |
| How is the request-body key picked (`body` / `json` / transactor generators)? | Probe fixed order: spec-named body param key first, else iterate `('body', sort keys generators)`, first present wins | Require explicit content-type/key; or honor only body/json | Lets json/body/xml all work w/o client knowing custom formats (Transactor generators). `sort` makes it deterministic; first-match-wins precedence (body then alpha) is implicit/undocumented. This is the open conflict r3-body-param-key-wins vs r3-body-content-key-wins. `Client.pm:142-151,208` | **covered (open conflict)** |

### 1D. Grammar / parser-architecture rationale (tree-sitter-pod, tree-sitter-perl)

| Question | Chosen | Alternative | Grounded rationale (refs) | Status |
|---|---|---|---|---|
| (pod) Parse =over/=item/=back FLAT vs nested over_block? | Flat sibling command_paragraphs, no enclosing block | Nest item/back inside =over (structurally "correct") | Grammar is embedded by tree-sitter-perl; =over and =back can land in different =pod…=cut regions split by Perl code → a nesting parser fails to find the close. Flat is forced by embedding. R3-VoV thread re-litigated this in 2026, verdict2 = original 2023 commit. `git:4b7f3ba`; `over-item-back-flat`; `r3-indented-block-verdict2` | **covered** |
| (pod) `/[ \t]*/` vs `/\s*/` after a command? | Explicit `[ \t]*` (and `[ \t]+`) | `/\s*/` shorthand (original) | `\s` includes linefeed → =back swallowed trailing newline + blank line + next paragraph's content. tree-sitter regex lacks Perl `\h`, so spell out `[ \t]`. Forced by a tooling limitation. `git:cc8fac8`; grammar.js comment | **GAP** |
| (pod) External scanner vs pure CFG? | External scanner emitting line-level tokens (_start_command/_plain/_verbatim/_data_section) | Pure grammar.js regex/precedence | POD types disambiguated by column-zero context (CFG can't test "am I at col 0"); chevron-run matching is context-sensitive (close count = open count). Survived every commit since first scanner. `scanner.c:132,227-248`; `git:e9db194`; `r3-external-scanner-design` | **covered** |
| (pod) Generic `command_paragraph` (`/=[a-zA-Z]\S*/`) vs enumerating each directive? | Single generic rule, only =pod/=cut/=begin/=end/=for special-cased | Enumerate each known directive as its own typed node (original) | cc1411d flipped it: enumerating errored on unknown/custom/future directives; generic accepts open-ended POD command space at the cost of per-directive node types (highlighting keys off token text). Corpus tracks as scope expansion, not as why-X-over-Y. `git:cc1411d`; `scope-generic-command-paragraph` | **covered** (gap: trade-off framing) |
| (pod) E<...> its own escape_sequence node vs reuse interior_sequence? | Distinct escape_sequence; `_escape_content=repeat1($._content_plain)`; scanner emits INTSEQ_ESCAPE_LETTER for 'E' | Treat E<...> like S<>/B<>/C<> via interior_sequence (the original "hack") | E<...> content is an entity name/number, not nested markup; re-entering generic parsing would wrongly parse markup inside an entity. repeat1 forces scanner to see CONTENT_PLAIN co-valid with INTSEQ_END. v1.1.0, d4e6745. `git:d4e6745`; `escape-sequence-distinct-node` | **covered** |
| (pod) `.gitignore` generated parser.c vs commit it? | gitignored; run `npx tree-sitter generate` | Commit parser.c (common, lets consumers build w/o CLI) | Keeps repo free of huge machine diffs, avoids parser.c drifting from grammar.js; cost: every consumer/CI runs the CLI. `README.md`; `generated-c-not-in-git` | **covered** |
| (perl) Homegrown integer TERMPREC ladder vs tree-sitter built-in partial-ordered prec? | Integer ladder LOW=0…PAREN=26, tier-for-tier from perly.y | tree-sitter built-in precedence names w/ partial ordering | CLAUDE.md: integer ladder "strictly simpler than tree-sitter's builtin and supported partial ordering — we tried." Total order mirrors perly.y 1:1. Cost: hand-maintained, must be synced to perly.y. `grammar.js:7-37`; `CLAUDE.md:88-90` | **GAP** |
| (perl) Enforce non-associativity in a GLR grammar with no native non-assoc? | `binop.nonassoc`: optional tail (repeated op + zero-width `_NONASSOC` + `_ERROR`) steers tree-sitter into an error branch | prec.left/right alone (no non-assoc primitive); or post-process tree | No tree-sitter non-assoc primitive → catch illegal chains grammatically. Duplicated operator is load-bearing ("don't simplify it away"). Reused for NCEQOP/NCRELOP/DOTDOT. `grammar.js:44-59,608,632,639`; `CLAUDE.md:91-94` | **GAP** |
| (perl) Disambiguate regex-leading `[`/`{` from subscript: open-time lookahead vs col+flag? | Single bounded lookahead at opening delimiter ("is first real body char the delimiter again?") | Prior: body-start column + per-quote `seen_content` bool | Columns repeat across lines (cross-line collision); seen_content can't see grammar-lexed interpolation (`$h{k}`). Lookahead answers definitively once. −39 lines, 228/228 tests, behavior identical. `git:2b18a88`; `scanner.c:767-787` | **GAP** |
| (perl) Optimize parser size by parser.c bytes or by state counts? | Optimize `LARGE_STATE_COUNT`/`STATE_COUNT`; factor shared hidden rule + `alias()` back to node names | Chase parser.c byte size; or use `supertypes:` to shrink table | parser.c bytes dominated by unicode tables (misleading proxy); LARGE_STATE_COUNT drives table size. `supertypes:` are schema-only, ZERO parse-table effect. Real wins from factoring repeated shapes via alias() (8592a2b: −314 large states). `CLAUDE.md:34-48,104-111`; `git:8592a2b,a1356a5` | **GAP** |
| (perl) Model `not EXPR` as a function call or a low-prec prefix logical op? | `logical_not_expression` at _term, NOTOP tier between ANDOP and LSTOP, operand=listexpr | Leave as ambiguous_function_call (call to sub `not`) | perly.y models `not` as `term: NOTOP listexpr` (a *term*, can sit on RHS of assignment), binding looser than comma, tighter than and/or. Siblings and/or/xor are _expr; `not` deliberately placed as _term to match perly.y. `git:a08cce2`; `grammar.js:650-655,13` | **GAP** |
| (perl) Which tree-sitter CLI + rebuild discipline for local dev? | cargo-installed CLI (0.26.x); `-r` when only scanner.c changes; per-worktree `TREE_SITTER_LIBDIR` | bundled prebuilt CLI / `npx` / node_modules binary w/ default freshness | Bundled CLI needs GLIBC 2.39 (won't run on older linux); cargo build does. CLI checks grammar.js/parser.c freshness but NOT scanner.c → stale perl.so → hence `-r`. .so cached by grammar *name* → parallel worktrees fight one file → per-worktree LIBDIR isolates. `CLAUDE.md:10-31` | **GAP** |

### 1E. LSP-extension / binary-acquisition rationale (zed-perl)

| Question | Chosen | Alternative | Grounded rationale (refs) | Status |
|---|---|---|---|---|
| Get PerlNavigator binary via npm or GitHub release? | Download prebuilt binary via `zed::latest_github_release("bscan/PerlNavigator")` | Install perlnavigator-server npm pkg (original f845a03) | npm route forced an ugly oniguruma node_modules copy hack; switched within ~23 min (88471b7) and removed the hack next commit. GitHub binaries sidestep node module resolution (no Node runtime/nested node_modules), trading away npm auto-version-tracking. Framed as stopgap "until next npm version." `git:88471b7,f845a03,bc94c30`; `perl.rs:28-35` | **covered** |
| Always download, or prefer a user binary on PATH? | `worktree.which("perlnavigator")` first, return immediately; cache-check before re-download | Always self-manage/download (pre-011deca) | 011deca; comment "don't bother installing if user already has it." README tells users to install on $PATH "until the next release" → PATH-first makes the documented manual workflow work. Trade-off: stale PATH binary silently wins (no version check). `perl.rs:14-22`; `git:011deca`; `path-first-binary-resolution` | **covered** |
| Cleanup old versions: targeted delete vs wipe-everything-but-current? | After download, remove_dir_all every entry != version_dir, swallow errors `.ok()` | Track/delete known-stale dirs; or skip cleanup | Blunt "delete non-current" simpler than bookkeeping, guarantees single copy; cost: nukes any unrelated file in work dir. Runs only in download branch. Errors ignored so failed cleanup never blocks LSP start — availability over tidy disk. `perl.rs:84-91`; `old-cleanup-deletes-stale-versions` | **covered** |
| Express "exactly one `e` modifier" in a tree-sitter query? | Two predicates: `(#match? "e")` AND `(#not-match? "e.*e")` | Single predicate asserting exactly-one (counting/lookahead) | tree-sitter `#match?`/`#not-match?` can't count or use lookahead → decompose into ≥1 AND not≥2. `ee` (eval-of-eval) left unhandled deliberately. `injections.scm:8-14`; `injection-regex-expressibility` | **covered** |
| Start PerlNavigator over stdio or another transport? | Resolved binary + single `--stdio` arg, empty env | (npm era) node_binary_path + script path + --stdio; or socket/port | stdio is conventional Zed/LSP transport, no port allocation; simplification is direct consequence of dropping Node-script indirection after self-contained binary. `perl.rs:101-114`; `lsp-started-with-stdio` | **covered** |
| Where does `.zip` live (find-asset vs in-archive binary path)? | asset_name WITHOUT suffix; append `.zip` only at find(); use bare asset_name as in-archive subdir | Bake `.zip` into asset_name once, reuse for both (pre-011deca) | Single-var leaked `.zip` into binary_path → looked for a literal `…zip/` subdir = real bug. 011deca split responsibilities. The zip-was-dir-path-bug correction. `git:011deca,88471b7`; `perl.rs:37-58` | **covered** |

### 1F. Composer-reimplementation rationale (composr)

| Question | Chosen | Alternative | Grounded rationale (refs) | Status |
|---|---|---|---|---|
| Shell out to real composer for behavior-as-spec phases vs port everything to Rust? | Hybrid: beat composer on mechanical/parallel phases natively, delegate runtime-dependent phases (EventDispatcher class-method handlers, first-cut bootstrap) to `composer run-script`/`dump-autoload` | Port everything from start; or delegate everything composer-shaped forever | Degrades gracefully: Composer::locate→None ⇒ warn + native fallback. NOT permanent — a711232/9980d3c clawed phases back into Rust once byte-output understood (monolith-app → 0 composer calls). class-method handlers stay delegated (reimplementing EventDispatcher is out of scope). `git:dd8dd49,a711232`; `README.md:122-150`; `install.rs:620-649` | **covered** |
| Keep delegating `dump-autoload` (~145s, byte-correct) or natively emit the 13 bootstrap files? | Natively emit all 13 vendor/composer/* files mirroring composer's templates + exact path styling | Keep delegating (guaranteed byte-correct, composer rescans every PHP file) | service-app cold install 176s→45s (~10x). Risk paid down by matching exact output shape (PSR-4 first-char bucketing + desc-length sort; `$vendorDir` vs `__DIR__` path styles) + golden byte-equality. `git:a711232`; `bootstrap.rs:111-134,471-499` | **covered** |
| SPI hits an unknown `Nevay\SPI` requirement attribute: drop or keep the provider? | Conservatively KEEP (`has_unknown_requirement=true`); accept over-inclusion | Drop unrecognized (risk silent missing service); or bail to composer on any unknown (slower) | Over-include = tolerable; dropping one composer kept = silent functional regression. Same posture in version-constraint parsing (`~X.Y`, spi.rs:516). Module stance: "when unsure, keep, let runtime decide." `spi.rs:225-247,516-541` | **GAP** |
| Lock content-hash: plain JSON serialization vs reproduce PHP json_encode byte shape? | Custom PHP-shaped encoder (`\/` escaping, lowercase `\uXXXX` + surrogate pairs, empty-obj→`[]`) + IndexMap preserve_order + top-level ksort + config.platform carve-out | serde_json default ("plain byte hash") | Reimpl diverged from PHP 3 ways (BTreeMap re-sort, missing slash/unicode escape, empty-obj) → stacked, NO real composer.json ever matched. Cross-validated vs `composer install --dry-run`. Plain hash is structurally incapable of matching. `git:1616e93`; `php_json.rs:1-16`; `content-hash-needs-php-shape` | **covered** |
| Replace old install dir with sync recursive-rm or move it off the critical path? | `rename()` old dir into `.composr-trash-<pid>-<n>/` (instant) + fire DETACHED `/bin/rm -rf` reparented to init | Sync empty_dir walking every file; or in-process bg thread (still blocks exit) | ~12s recursive rm = half the extract stage on a 127-pkg reinstall. Bonus crash-safety (extract to staging, swap at end → failed extract leaves old vendor intact). Cost: orphaned trash dirs on Ctrl-C between rename+spawn — mitigated by orphan scan at extract start. `git:de08edb`; `extract.rs:223-228,410` | **GAP** |
| Single mtime classmap cache, or add a second shared tier? | Add per-package SHARED cache (Tier 1) keyed (package_name, dist.reference) in XDG_CACHE that SURVIVES `rm -rf vendor`; demote mtime cache (Tier 2) to root + uncacheable pkgs | Keep only per-file mtime cache (simpler, but every vendor wipe re-walks/re-parses) | Same version ⇒ identical post-admission classmap → sound to key on dist.reference. monolith-app (292 pkg): cold-cold ~33s; vendor wiped w/ shared cache ~16s; one file touched ~600ms (now dominated by emitting 24MB/120k-entry classmap). Byte-identical hit/miss. `git:5ae7e16`; `classmap_cache.rs:24-44`; `two-layer-classmap-cache` | **covered** |
| Zip extraction: explicit huge/small partition (LPT) vs single par_iter? | Single `par_iter().with_max_len(1)` over size-sorted-desc list; dropped partition + COMPOSR_HUGE_THRESHOLD_MB knob | The huge/small LPT partition it replaced | Split "solving a non-problem": rayon work-stealing already balances the tail when input is sorted desc + each item is its own stealable task — same outcome, less code, no env knob. `git:a565c52`; `single-par-iter-extract`/`lpt-partition-extract` | **covered** |

---

## 2. Kludge / Known-Debt Bank

### 2A. robot-game

| Compromise | Forcing constraint | Severity | Ref | Status |
|---|---|---|---|---|
| WASM save timestamp hardcoded to 0 → date display can only show "Saved", never a real date | macroquad `get_time()` returns secs-since-start not epoch; real epoch "needs JS interop" not wired via miniquad plugin; date formatting "needs more deps" | low | `save.rs:94-97,221-226` | **GAP** |
| TTS is a no-op on every native build; only WASM speaks; native code path is dead weight kept for compilation | TTS via browser SpeechSynthesis as a miniquad extern "C" plugin; no native speech backend ("browser-only for now"); native-speech-spec.md unimplemented | low | `audio/tts.rs:32-39,63-69` | **GAP** |
| Hand-picked test seeds (seed 0) fragile: reordering any upstream rng.gen() shifts every downstream draw → test hangs in wait_until, not a clean fail. ADR-002 accepts as debt | All gameplay RNG in one shared seeded SmallRng (for determinism), so a seed encodes full draw order; mitigation ("force challenge" hook) deferred until "many such tests exist" | medium | `adr/002:64`; `tests/story.rs:80-83`; `git:5045e75` | **covered** |
| Legacy save migration = quiet best-effort: deprecated `math_band` folded in only if profile "looks default" (==1); a real band-1 kid won't overwrite; Dir deserialized from enum-name or legacy u8 | Old localStorage saves predate LearnerProfile/typed Dir; must keep loading across JS→Rust w/o a versioned migration system → serde(default) + untagged enum + in-place migrate_legacy() w/ a "looks default" guess | medium | `save.rs:24-27,51-74`; `git:163637b,b861391` | **GAP** |
| No cross-impl conformance test that LocalStorage & InMemory backends agree on edge cases; relies on "behavior simple enough drift is unlikely" | InMemory exists so parallel test doesn't race on /tmp; production backend can't run in same test process (extern "C" WASM / /tmp native) → no shared suite | medium | `save.rs:111-187`; ADR-002 Risks; `r3-save-backend-no-crosstest` | **covered** |
| Band-picker arrow click regions are loose hand-tuned rects, knowingly imprecise; "rough — original did the same loose rect" (carried over from JS prototype) | No layout/hit-test system; click regions hardcoded pixel rects ported as-is to avoid re-deriving hitboxes during migration | low | `ui/title_screen.rs:250-255` | **GAP** |
| Two headline features — GenAI dialogue and voice ("Say it") — fully specced but unimplemented; `src/net/` doesn't exist on disk; domain has VoiceResult support with no platform path | AI dialogue needs pre-gen/caching to hide 1–3s latency from a button-mashing 4-yo (>1.5s = "lost"); voice needs a browser SpeechRecognition plugin. Deferred infra → domain hooks sit dormant | low | `genai-spec.md:3-12`; `voice-input-impl-spec.md:5`; net/ absent | **GAP** |
| Spread-width rates (0.05 up, 0.1–0.15 down) + promote/stretch thresholds tuned by intuition; ADR flags low 0.65 promote + wide spread "could promote too aggressively," monitor via simulator | No real-kid playtest data (only CLI sim); adaptive dials seeded w/ hand-picked constants to ship, retune after playtesting | low | `adr/001` Risks; `per-subskill-bands-spec.md:77` | **GAP** |

### 2B. perl-openapi-client

| Compromise | Forcing constraint | Severity | Ref | Status |
|---|---|---|---|---|
| md5-fallback length threshold self-admittedly arbitrary: `md5_sum($package) if length > 110;  # 110 is a bit random` | Perl pkg names have no formal length limit but very long ones break (autosplit FS paths, unreadable traces); spec URL→class name → some arbitrarily long; cutoff needed, no principled value | low | `Client.pm:237`; `r3-url-to-class-md5-threshold` | **covered** |
| `+($param->{type}//'') eq 'array'`: leading unary `+` precedence kludge; `//''` was a forced fix after shipping without it and dying | OpenAPI allows absent `type` → `eq 'array'` warned/died on uninit; bare `+` needed so `?:` parses as a value not mis-binding to `\|\|` | low | `Client.pm:219`; `git:e2434a3,6aeb6c8`; `collection-format-type-check-bug` | **covered** |
| Query callback special-cases UNDEF `$name` = "JV wants whole params hash to reassemble exploded object"; comment cites private `JSON::Validator::…::_get_parameter_value` | No public protocol for deepObject/form-explode object params; client mirrors an undocumented JV calling convention; coupling to a private method → any JV refactor silently breaks it | medium | `Client.pm:174-187`; `git:faede03`; `deepobject-name-undef-protocol` | **covered** |
| Class gen runs an arbitrary spec URL through string `eval` (`package $class; use Mojo::Base '$parent'; 1;`), confess on fail; pkg name = sanitized/hashed URL | To get a real cacheable subclassable named-method package per spec w/ only Mojo primitives, string-eval is least-dep; correctness depends entirely on `_url_to_class` (`s!\W!_!g`) producing a legal package name | medium | `Client.pm:68-72,232-240` | **GAP** |
| `return unless $schema->can('routes')` — non-OpenAPI schema → generation silently produces a class with NO operation methods | JV->schema() can return a plain JSON Schema (no routes()); code degrades silently rather than error; 9d7f63f added it reactively after "Can't locate object method routes" | low | `Client.pm:75`; `git:9d7f63f` | **GAP** |
| `app => $app` local mode mutates already-built base_url in place: `->host(undef)->scheme(undef)->port(undef)` | Mojo::UA decides remote-vs-embedded by URL absoluteness; must blank host/scheme/port AFTER base_url computed from spec → order-dependent surgery on a shared lazy attr | low | `Client.pm:48-51`; `Command/openapi.pm:42-45` | **GAP** |
| Path-template substitution `s,\{([-\w]+)\},{$params->{$1}//''},ge` — missing required path param silently becomes empty segment | Templating runs BEFORE/independent of JV required-param validation → can't assume value exists; `//''` avoids uninit warning, defers "missing required" to validator; if validation bypassed → malformed URL | low | `Client.pm:133-134`; `t/path-templating.t`; `git:c51c5e4` | **GAP** |

### 2C. tree-sitter-pod

| Compromise | Forcing constraint | Severity | Ref | Status |
|---|---|---|---|---|
| Scanner ends a plain paragraph when next line starts `=` even with no blank line — accepts spec-violating POD ("so many people omit it. We'll allow this here") | Real-world POD frequently omits the required blank line before a command; strict parsing mis-parses common input | low | `scanner.c:321-330`; `plain-para-tolerates-missing-blank-line`; open conflict `r3-blank-line-lax` vs `r3-blank-line-spec` | **covered** |
| Chevron nesting >8 silently mishandled: chevron_count_top() returns 1 past MAX, so deep `C<<<<` closes on a single `>` → wrong parse, no error | Fixed `unsigned char[8]` chevron stack keeps serialize/deserialize a trivial fixed-struct memcpy, no heap; >8 depths deemed pathological | low | `scanner.c:51-57,102-107`; `git:ca68828`; `hunch-chevron-stack-overflow-implied-one`, `r3-max-nested-chevrons-8` | **covered** |
| `ADVANCE` macro renamed to `ADVANCE_C` purely to placate gcc | gcc objected to bare `ADVANCE` name ("to avoid upsetting gcc"); `_C` suffix otherwise meaningless | low | `git:663ae63`; `scanner.c:16-29`; `advance-c-side-effects-c` (captures side-effect, not gcc motivation) | **GAP** (motivation) |
| `did_zw_data` guard flag + return-false bailouts solely to stop tree-sitter spinning on zero-width tokens | tree-sitter infinite-loops if scanner repeatedly emits zero-length token at same position (empty data sections, unfinished intseqs, double/single-LF plain ends); no framework fix → hand-rolled per-site guard | medium | `scanner.c:56,169-173,306-329`; `git:5b58319,e6b02d1,c4f8662`; `reducer-zero-width-guard-pattern` | **covered** |
| `at_end_command()` peeks via advance() WITHOUT mark_end → destructively consumes; correctness depends on caller having called mark_end first | tree-sitter scanner API has no cheap non-consuming lookahead; advancing is the only inspection → relies on a documented caller contract, not self-contained | medium | `scanner.c:114-130,183-189`; `hunch-end-command-peek-destructive`, `data-section-checked-before-start-command` | **covered** |
| Interior-sequence (S<...>) shipped as self-described provisional "Initial hack"; surrounding plain-content logic accreted multiple infinite-loop fixes | Multi-line intseq content + chevron-boundary detection via external scanner is genuinely tricky; author shipped working-but-rough first cut then patched fallout (#2 loop, #4 intseq-in-verbatim, zero-width loops) | low | `git:2f83f42,e6b02d1,5f3639c`; `scanner.c:251-368` | **GAP** |

### 2D. zed-perl

| Compromise | Forcing constraint | Severity | Ref | Status |
|---|---|---|---|---|
| `move_oniguruma_super_evil()` recursively COPIES top-level node_modules/vscode-oniguruma into perlnavigator-server's nested node_modules; labelled "very super duper evil"/"don't be evil"; result discarded via `let _ignored = …`. Removed in bc94c30, npm approach abandoned | vscode-oniguruma wouldn't resolve in the nested npm layout zed::npm_install_package produced; no clean way to fix nested node module resolution from inside the extension sandbox | high | `git:f845a03,bc94c30`; `human-verdict-oniguruma-hack-was-wrong`, `hunch-oniguruma-necessary` | **covered** |
| GitHub-release binary download is itself a stopgap: "switch to github release UNTIL THE NEXT NPM VERSION IS OUT"; README "Until the next release…". No revert ever happened — silently became permanent | At the time the npm package couldn't be made to work cleanly (oniguruma); GitHub route adopted as a bridge pending an upstream fix never incorporated | medium | `git:88471b7`; README; `perl.rs:28-92` | **GAP** |
| Windows support added grudgingly ("grumble: windows support") — only appends `.exe`; aarch64 & x86 remain hard `Err`. Partial platform matrix | PerlNavigator release assets ship only x86_64 mac/linux/win; aarch64/x86 have no asset; extension can only support what upstream publishes | medium | `git:4a173b0`; `perl.rs:40-50,60-66`; `aarch64-unsupported`, `windows-exe-extension` | **covered** |
| Post-download cleanup deletes every non-current entry via `remove_dir_all(...).ok()` — blunt wipe + silent error-discard; unexpected siblings destroyed, partial delete invisible | Wants exactly one resident version w/ minimal bookkeeping; cleanup failure must never abort LSP start → disk hygiene sacrificed for availability | low | `perl.rs:84-91`; `git:011deca`; `old-cleanup-deletes-stale-versions` | **GAP** |
| Perl query files lifted from nvim-treesitter ("; an scm file for nvim-treesitter" header still present); nvim capture names (@conditional/@repeat/…) retrofitted to Zed's @keyword.* later; stale provenance comment is leftover debt | Hand-writing complete highlight/injection queries is expensive; reusing nvim's was the fast path, but nvim/Zed use different capture conventions → follow-up correction + misleading comment | low | `injections.scm:1`; `git:4eef601`; `wrong-capture-names-initially` | **covered** |
| `s///e exactly-one-e` injection silently does NOT handle the double-e (ee) case — no rule, just unmatched. Corpus previously confabulated a separate "ee" rule | `#match?`/`#not-match?` can't count or use lookahead → distinguishing e/ee/more beyond the one decomposed pair is awkward; ee left unhandled | low | `injections.scm:8-14`; `injection-regex-expressibility` | **covered** |

### 2E. composr

| Compromise | Forcing constraint | Severity | Ref | Status |
|---|---|---|---|---|
| `platform_check.php` shipped as empty no-op stub instead of real php-version + ext-* checks | Lock's platform reqs not wired to bootstrap writer; autoload_real.php's `require platform_check.php` blows up without at least an empty file → stub is the minimum keeping the autoloader loadable | medium | `bootstrap.rs:119-127`; `git:a711232` | **GAP** |
| Empty PSR-4 prefix is composer's `fallbackDirsPsr4` but composr stuffs it into `prefixDirsPsr4`; justified by "we don't see it in service-app/monolith-app" | Correctness scoped to two real target apps; empty-prefix is rare/absent there → faithful fallback deferred; an app using empty PSR-4 prefix gets non-byte-equal/divergent autoload | medium | `bootstrap.rs:483-496` | **GAP** |
| Class-method script handlers (`Foo\Bar::baz`) and unrecognized `@<event>` aliases are warn-and-skipped on native path (silent no-op unless composer reachable) | Class-method handlers need Composer's EventDispatcher/runtime (deliberately not reimplemented); for @-aliases, service-app has none at install-time → "warn-and-skip is fine" | low | `scripts.rs:115-140`; `README.md:147-150` | **GAP** |
| Native SPI replication silently bails (warn → composer delegation) for any pkg using `extra.spi-config.autoload-files` | autoload-files would require running PHP at install time, which composr's no-PHP premise forbids → can't reproduce natively, must delegate | low | `install.rs:487-509`; `README.md:174-176` | **GAP** |
| SPI provider scan conservatively KEEPS providers w/ unevaluable requirement attributes ("we may over-include") | composr's static analyzer can't evaluate every `Nevay\SPI` attribute shape; composer evaluates at runtime w/ PHP, composr doesn't → guesses, choosing over-inclusion as safer-wrong; known byte-inexactness | low | `spi.rs:230-233` | **GAP** (pairs w/ 1F SPI rationale) |
| Non-Unix (Windows) path-package symlinking unimplemented — `try_symlink` always errors, falls through to full directory mirror (copy) | Windows symlinks need junctions + elevation "we don't bother with"; explicit decision not to pay the Windows symlink-API cost for a Unix-targeted tool | low | `extract.rs:78-83` | **GAP** |
| README `--strict-plugins` says default is "install plugins as inert files and warn" — contradicts actual default (delegate unknown-plugin lifecycle to `composer run-script`) and its own Plugin-policy section | Docs drifted from the three-tier plugin policy as it evolved (delegate-all → three-tier); flag parenthetical never updated | medium | `README.md:204-205,182-185`; `composr-doc-defects`, `unknown-plugins-delegate-not-inert`, `doc-strict-plugins-default-inert` | **covered** |
| `classmap_cache CACHE_VERSION` must be hand-bumped whenever admission semantics change, even when on-disk struct shape is unchanged — manual coupling between filter.rs logic and a const | Cached data is post-admission FQCN→file pairs; an admit() semantic change silently invalidates entries with no structural/serde signal; only guard is a human bumping the integer | low | `classmap_cache.rs:24-31` | **GAP** |

### 2F. tree-sitter-perl

| Compromise | Forcing constraint | Severity | Ref | Status |
|---|---|---|---|---|
| `__DATA__`/`__END__` EOF detection needs a CTRL-Z (`\x04`/`_ctrl_z_hack`) branch aliased to eof_marker — solely so the grammar compiles on Windows | Without it tree-sitter "can't compile on windoze" (Windows treats CTRL-Z/EOT specially); these markers don't create a __DATA__ filehandle so their content stays hidden | low | `grammar.js:148,977-982` | **GAP** |
| Autoquote scanner's whitespace/comment-skip loop acknowledged-incomplete: doesn't handle a 2-char quote needing comment-char guarding; doesn't skip POD that could legally appear ("EYES ROLL") | Quoting is space-sensitive and POD can interleave with code; full handling needs 2-char-quote tracking + a POD skip the scanner doesn't do | medium | `scanner.c:1126-1128,1141` | **GAP** |
| `$.variables` excluded from `supertypes:` for no understood reason ("i don't know why, but these just went crazy") — a public-supertype capability dropped because enabling it broke generation undiagnosed | Adding it caused unexplained grammar blow-up; root cause never found → commented out | low | `grammar.js:103` | **GAP** |
| Scanner state defensively zeroed in two memory-safety fixes: stale heredoc_delim bytes (len 0 but dirty contents) + uninitialized stack-local TSPString; both via valgrind | tree-sitter memcmps the SERIALIZED scanner state for GLR branch merging + zero-width rejection → any stale/uninit byte prevents identical states merging or causes uninit reads; serialize/deserialize must be byte-clean | high | `git:71b727e,bfdf528,951e607` | **GAP** |
| Comment-skip loop pinned CPU at 100% (infinite loop) when a file ends with a comment + no trailing newline — `while(get_column) ADVANCE_C` never reaches col 0; advance() is a no-op at EOF | At EOF advance() is a no-op and column never returns to 0 → column-only loop can't terminate; needed explicit `!lexer->eof(lexer)` guard | high | `git:d5ae131`; `scanner.c:1138` | **GAP** |
| First list/non-assoc attempt (#47) reverted next commit (#48), then reworked entirely; final solution is the error-branch/duplicated-operator trick, not native support | tree-sitter has no native non-/list-associativity → straightforward impl didn't hold up, reverted + replaced with homegrown `_NONASSOC`+`_ERROR` | medium | `git:ec7d02f,4a55570,a1356a5` | **GAP** |
| Many rules forced into GLR conflicts for mechanical lookahead, not structural ambiguity (postfixy autoquotes, hiding `->`, nameless params, continue-BLOCK-vs-func0) | tree-sitter LR core gives limited lookahead; several Perl constructs need extra tokens to decide → declared as conflicts to push into GLR | low | `grammar.js:196-213` | **covered** |

---

## 3. Per-Corpus FETCH RECORD (round provenance)

| Corpus | repo_available | Key sources read | Greps run |
|---|---|---|---|
| **robot-game** (`/home/veesh/personal/robot-game`) | true | CLAUDE.md; ADR-001 (band-blending), ADR-002 (headless harness); save.rs (1-75,80-140,200-239); audio/tts.rs; ui/title_screen.rs:240-269; domain movement.rs:40-90; game.rs (1095-1122,2355-2389); follower.rs:1-55; genai-spec.md, voice-input-impl-spec.md, operation-taxonomy-spec.md:366-484, per-subskill-bands-spec.md; git log -50 + spike bodies f036c78/62e28d7; corpus INDEX + sampled beliefs | `rg FIXME\|HACK\|TODO\|for now\|kludge\|…` over rs/md/toml/sh; `migrate_legacy\|legacy\|serde.*default`; `phase_through_entities\|phasing`; `for now\|nulls for now\|adapter` in taxonomy spec; `ai_dialogue\|fetch\|reqwest\|extern "C"` |
| **perl-openapi-client** (`/home/veesh/personal/perl-openapi-client`) | true | lib/OpenAPI/Client.pm (full + POD); Mojolicious/Command/openapi.pm (first 60); README; Changes (full); INDEX + 5 belief files; git show 810f532/9d7f63f/f43fe79/faede03/6aeb6c8/e2434a3/0d648f7/200c2d5 | `rg -i FIXME\|HACK\|for now\|wrong\|broken\|sigh` (4 hits, all deepObject); git log -50; log -5 on Client.pm; log grep `local\|app` |
| **tree-sitter-pod** (`/home/veesh/personal/tree-sitter-pod`) | true | corpus INDEX + 12 sampled beliefs; grammar.js; src/scanner.c (full, 372 lines); README; git log -50 + scanner.c/grammar.js logs; git show 4b7f3ba/cc8fac8/663ae63/2f83f42 | `rg -i FIXME\|HACK\|note:\|caveat\|nasty\|ick` (hits: scanner.c:190,322, node/index.js:7, array.h:155); log grep revert/workaround |
| **zed-perl** (`/home/veesh/personal/zed-perl`) | true | corpus INDEX; src/perl.rs; extension.toml; README; injections.scm; git show f845a03/bc94c30/88471b7/011deca/4a173b0/4eef601; 7 belief files | `rg -i FIXME\|HACK\|evil\|super_evil` (current tree clean — signals only in git history); find for md/CLAUDE/adr/design (only README); git log -50 (13 commits) |
| **composr** (`/home/veesh/personal/composr`) | true | corpus INDEX + README + 2 beliefs; repo README:120-209; bootstrap.rs (119-127,483-496); scripts.rs:115-144; install.rs (626-649,487-509); spi.rs:225-247; extract.rs (62-69,78-83); classmap_cache.rs:24-31; php_json.rs:1-16; parse.rs:62-77; git log -50 + bodies a565c52/de08edb/4a41997/dd8dd49/a711232/1616e93/5ae7e16 | `rg -i FIXME\|HACK\|for now\|hack`; `TODO\|FIXME\|…`; `not support\|unsupported\|naive\|best.effort\|conservativ`; `why\|chose\|instead of\|trade.?off\|delegat` over README; `platform.?check\|stub\|symlink\|detached\|trash` over beliefs |
| **tree-sitter-perl** (`/home/veesh/personal/tree-sitter-perl`) | true | corpus INDEX + README; CLAUDE.md; grammar.js (1-44,44-70,95-110,196-236,608-670,970-990); scanner.c (765-790,1118-1148); git show 2b18a88/71b727e/d5ae131/ec7d02f/a1356a5; log for a08cce2 | `rg -i FIXME\|HACK\|for now\|TODO`; `note that\|in theory\|sigh\|hate\|annoying\|weird`; `_ctrl_z_hack\|x04\|x1a`; `binop.nonassoc\|_NONASSOC\|_ERROR`; `conflicts:`; `TERMPREC\|we tried\|builtin`; log grep shrink/prec/nonassoc/revert/infinite/valgrind |
| **private-crm** *(private)* | not in round | — (no harvest data this round; private third-party code, examples must be marked **(private)** if surfaced) | — |

---

## 4. Targets for the Next Build Round

### Highest-value gaps to turn into beliefs first

**Tier 1 — "messy real-world knowledge" with sharp forcing constraints + clear severity (build first):**
1. **tree-sitter-perl — EOF infinite-loop (`d5ae131`) + valgrind state-zeroing (`71b727e`/`bfdf528`)** — two `high`-severity, mechanism-rich debts (advance()-no-op-at-EOF; tree-sitter memcmps serialized GLR state → byte-cleanliness mandatory). Generalizable scanner-authoring lessons; nothing in corpus.
2. **zed-perl — GitHub-release-as-permanent-stopgap** — `medium`; a textbook "temporary became permanent" debt with explicit commit/README intent that was never honored. Cross-references the (covered) oniguruma verdict.
3. **composr — `platform_check.php` no-op stub + empty-PSR-4-misfiled-as-prefixDirsPsr4** — two `medium` byte-inexactness debts scoped to "two target apps," precisely the kind of correctness-vs-scope compromise the corpus under-captures.
4. **robot-game — legacy save "looks default" migration heuristic** — `medium`; an explicit guess-based migration that can misfire (real band-1 kid), forced by absence of a versioned migration system.

**Tier 2 — design-rationale why-X-over-Y with a *tried-and-abandoned* alternative (strongest belief shape):**
5. **tree-sitter-perl — integer TERMPREC ladder ("we tried" the built-in partial order)** and **non-assoc via error-branch** and **open-time-lookahead vs col+flag** — three crisp why-over-alternatives, each with the alternative actually attempted/measured. Richest single untapped vein in the round.
6. **robot-game — accuracy-window vs streak promotion** and **distribution vs hard/half bands** and **pure step()/render() vs headless macroquad** — three high-quality ADR-grounded rationales with named, rejected alternatives and observed failure data.
7. **robot-game — zero-serialization Rust path-dep vs JSON/WASM bridge** — whole-failure-class-elimination rationale w/ concrete drift commits.
8. **composr — async-trash detached rm** and **SPI conservative-keep** — measured trade-off + a "safer-wrong-answer" stance, both gap.
9. **perl-openapi-client — base_url coercion *why*** and **per-parent cache-key consequence of `$BASE` removal** — fill the why/consequence layer beneath already-covered facts.

### Richest vs thinnest corpora in these two flavors

- **Richest (most high-value, un-captured material):**
  - **tree-sitter-perl** — by far the densest untapped seam: a rich CLAUDE.md of design rationale + multiple high-severity scanner debts, and the corpus is currently a tiny adversarial-WRONG-belief harvest → nearly everything here is a gap.
  - **robot-game** — two well-structured ADRs with mined-but-not-captured "Alternatives Considered" sections; many gaps in both flavors with observed-failure data.
  - **composr** — unusually detailed commit bodies (why + measured numbers); the corpus captures the WHAT but rarely the why-over-named-alternative or the constraint-driven kludges (most kludges are gaps).

- **Thin (well-mined or low-yield this round):**
  - **zed-perl** — tiny single-file extension; most rationale/kludges already covered; only ~3 gaps, and all kludge signals live in git history (scrubbed from the working tree).
  - **tree-sitter-pod** — already remarkably dense (35 beliefs on a small repo); most surface kludges covered; genuine gaps are narrow (gcc ADVANCE_C *motivation*, `\h` whitespace workaround, the "Initial hack" provenance).
  - **perl-openapi-client** — polished CPAN module with essentially no explicit debt markers; strongest items already covered; remaining gaps are why/consequence *layers* beneath existing facts rather than new facts.

- **private-crm (private):** no harvest data this round — should be scheduled for its own fetch pass; any examples it yields must be marked **(private)** and (per memory) its corpus stays gitignored.

**Build-order recommendation:** lead with tree-sitter-perl (Tier-1 high-severity debts + Tier-2 "we tried" rationales), then robot-game's ADR rationales and the save-migration debt, then composr's scope-compromise kludges. These maximize both belief quality (named rejected alternative / sharp forcing constraint) and future eval-ask value, while zed-perl and tree-sitter-pod can be deprioritized as largely covered.